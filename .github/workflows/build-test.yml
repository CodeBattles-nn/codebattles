name: build-test
on: 
  pull_request:
    branches:
      - master

jobs:
    build:
        name: build
        runs-on: self-hosted
        steps:
        - uses: actions/checkout@v1
        - name: build
          run: docker compose -p test_codebattles__ build
          
